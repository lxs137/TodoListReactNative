import React, { Component } from 'react';
import { Text, View, ListView } from 'react-native';
import { styles } from '../styles/list';

export default class TodoList extends Component {
  render() {
    return (
      <Text>Hello Todo List!</Text>
    );
  }
}